import { FC } from "react";
import { Layout } from "../../components";
import { UpdateBadge } from "../../components/UpdateBadge/UpdateBadge";

export const UpdateBadges: FC = () => {
  return (
    <Layout>
      <UpdateBadge />
    </Layout>
  );
};
