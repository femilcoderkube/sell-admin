import { FC } from "react";
import { Layout } from "../../components";
import { AddPartner } from "../../components/AddPartner/AddPartner";

export const AddPartners: FC = () => {
  return (
    <Layout>
      <AddPartner />
    </Layout>
  );
};
