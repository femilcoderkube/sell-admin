import { FC } from "react";
import { Layout } from "../../components";
import { UpdateRule } from "../../components/UpdateRule/UpdateRule";

export const UpdateRules: FC = () => {
  return (
    <Layout>
      <UpdateRule />
    </Layout>
  );
};
