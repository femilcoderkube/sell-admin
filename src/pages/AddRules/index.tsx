import { FC } from "react";
import { Layout } from "../../components";
import { AddRule } from "../../components/AddRule/AddRule";

export const AddRules: FC = () => {
  return (
    <Layout>
      <AddRule />
    </Layout>
  );
};
