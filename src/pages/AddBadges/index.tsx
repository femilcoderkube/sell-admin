import { FC } from "react";
import { Layout } from "../../components";
import { AddBadge } from "../../components/AddBadge/AddBadge";

export const AddBadges: FC = () => {
  return (
    <Layout>
      <AddBadge />
    </Layout>
  );
};
