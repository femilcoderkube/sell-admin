import React from "react";

import { FC } from "react";
import { Layout, Rule } from "../../components";

export const Rules: FC = () => {
  return (
    <Layout>
      <Rule />
    </Layout>
  );
};
